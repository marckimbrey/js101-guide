import expect from 'expect'
const file = require('../../lib/file')

describe('File', () => {
  it('should get the output of a file', () => {
      expect(true).toEqual(true) // @todo
  })
  it('should compare the output of two files', () => {
      expect(true).toEqual(true) // @todo
  })
})
