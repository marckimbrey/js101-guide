import expect from 'expect'
import { foo } from '../../guides/welcome'

describe('Welcome guide', () => {
  it('should have a solution', () => {
      const expected = true
      const actual = true
      expect(expected).toEqual(actual)
  })
});
